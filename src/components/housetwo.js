const Housetwo = ({ housetwo, btnWindows2 }) => {
  return (
    <div className="house-two">
      {housetwo.window1 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👮‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window2 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🧛‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window3 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🧝‍♀️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window4 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👩‍🍳
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window5 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👩‍🎤
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window6 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👩🏽‍🎨
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window7 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👨‍💼
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window8 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👨‍🔧
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window9 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👩‍🎓
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window10 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🤷‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window11 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          👩‍💼
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window12 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          💂‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window13 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🧞‍♀️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window14 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🦸🏼‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window15 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🤵
        </button>
      ) : (
        <button></button>
      )}
      {housetwo.window16 ? (
        <button
          onClick={(e) => {
            btnWindows2(e);
          }}
          disabled={false}
        >
          🧘
        </button>
      ) : (
        <button></button>
      )}
    </div>
  );
};

export default Housetwo;
