const Houseone = ({ houseone, btnWindows1 }) => {
  return (
    <div className="house-one">
      {houseone.window1 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🧙‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window2 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👨‍🎨
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window3 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🧞‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window4 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🧝
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window5 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👨‍🏭
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window6 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👨‍🌾
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window7 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👨‍🎤
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window8 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👩‍🔬
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window9 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🧕
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window10 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🤹‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window11 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🦹‍♂️
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window12 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🦇
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window13 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👩‍🏫
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window14 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👰
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window15 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          🧙‍♀️
        </button>
      ) : (
        <button></button>
      )}
      {houseone.window16 ? (
        <button
          onClick={(e) => {
            btnWindows1(e);
          }}
          disabled={false}
        >
          👨‍⚖️
        </button>
      ) : (
        <button></button>
      )}
    </div>
  );
};

export default Houseone;
